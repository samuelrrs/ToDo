import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  noTask: {
    color: 'gray',
    textAlign: 'center',
    fontSize: '18px',
    marginTop: '20px',
    marginBottom: '20px',
  },
}))

export default useStyles
